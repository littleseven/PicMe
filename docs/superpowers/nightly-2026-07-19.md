# 夜间自主工作总结 — 2026-07-19

> 你睡前授权"自动完成所有工作，明早看结果"。这是汇报。

## TL;DR

- **热守卫（①）**：好消息——**代码里已经存在且完善**，不用重做（我之前判断错了）。
- **SmolVLM2-500M**：下载完整（598M）+ 配置分析 + 兼容性确认，**只差设备实测中文**。
- **障碍**：设备 ~23:50 断开（USB/休眠），SmolVLM 中文 go/no-go 没能跑。模型和步骤都备好了，明早设备一恢复 10 分钟出结果。

---

## 1. 热守卫 — 已存在且完善（无需重做）

我睡前以为要从零写，实际查代码发现 `TagGenerationService` 早就实现了完整的热/电守卫：

| 我原计划做的 | 现状 |
|---|---|
| 接 PowerManager 热状态（MODERATE→PAUSE / SEVERE→ABORT） | ✅ `checkGuard()` `TagGenerationService.kt:477-499` 已实现 |
| charging 约束（不充电+低电停） | ✅ `checkGuard()` 电池档 ABORT/PAUSE + `BatteryManager` 监听 `:260-274` |
| 自适应节流 | ✅ `getAdaptiveThrottleMs()` `:465-475`（MODERATE→3s, LIGHT→300ms, NONE→50ms） |
| OpenCL 默认关 | ✅ `tagGenerationUseOpencl` 默认 `false` `UserPreferencesRepository.kt:638` |
| **idle/息屏约束** | ❌ 唯一缺口（次要，可选增强） |

**结论**：①实质已完成。唯一没做的是"只在息屏时扫描"——但相册打标本就可亮屏后台跑，这条非必须。建议先观察实际发热，真需要再加。

---

## 2. SmolVLM2-500M — 下载完整 + 兼容性确认

### 下载产物（`/tmp/smolvlm2_500m_mnn/`，598M，完整）
```
config.json          # MNN-LLM 主配置：backend cpu, thread 4, mllm 视觉后端
llm_config.json      # is_visual:true, 512px, Qwen 风格 chat template
llm.mnn / llm.mnn.weight          # LLM 主体（500M, 8bit 量化）
visual.mnn / visual.mnn.weight    # 视觉编码器
tokenizer.txt / embeddings_bf16.bin
```

### 关键发现：接入兼容性高
1. **chat template 是 Qwen 风格**（`<|im_start|>` + System/User/Assistant + `<end_of_utterance>`），和项目现有 `promptProvider` 接近，prompt 基本能复用。
2. **视觉自动加载**：`MnnLlmClient.createRuntimeConfig`（`MnnLlmClient.kt:450-454`）在 config 缺 `visual_model` 时自动补 `visual.mnn`/`visual.mnn.weight`——SmolVLM 文件结构正好匹配，**现有 `imageInference`/`generateWithImage` 路径应能直接加载 SmolVLM**，不用改多模态管线。
3. modelscope 元数据标了 `language: cn`（但这是上传者标的，模型实际中文能力仍需实测）。

### 唯一未知：中文质量
SmolVLM 训练语料以英文为主，500M 中文打标质量是 go/no-go 关卡。**必须设备实测**。

---

## 3. 障碍：设备断开

- ~23:50 `adb` 报 `no devices/emulators found`，3 次重连（kill-server/start-server）无果
- 判断：USB 物理断开或手机休眠关了 adb（你睡了没法重连）
- 影响：SmolVLM 模型没法 push 到设备、没法跑推理、中文 go/no-go 没出

---

## 4. 明早第一件事：验证 SmolVLM 中文（约 10 分钟）

设备恢复后，按顺序跑：

**步骤 1 — push 模型到设备 app 私有目录**
```bash
adb push /tmp/smolvlm2_500m_mnn /sdcard/smolvlm2_500m
adb shell "run-as com.mamba.picme sh -c 'mkdir -p files/llm_models/smolvlm2_500m && cp -r /sdcard/smolvlm2_500m/. files/llm_models/smolvlm2_500m/ && ls files/llm_models/smolvlm2_500m/ | head'"
```

**步骤 2 — 临时切 MODEL_KEY（一行）**
编辑 `app/src/main/java/com/mamba/picme/domain/tag/TagGenerationScheduler.kt:76`：
```kotlin
private const val MODEL_KEY = "smolvlm2_500m"   // 原值 "qwen3_5_2b"
```
然后：`./gradlew :app:installDebug`

**步骤 3 — 跑扫描，看输出**
- 触发扫描：进 Tag Viewer 页（设置→相册功能→相册调试功能→图片下载页→开发测试页→查看生成标签），或用 `start_tag_scan`
- 也可直接对一张图重打标看输出
- 看 logcat：`adb logcat -s PoLang:TagScheduler PoLang:LlmJNI`

**判定**：
- 中文标签合理（场景/物体/标签/摘要通顺）→ 全面接入（OpenCL 守卫适配 + 批量回归 + Tag Viewer 对比 2B）
- 中文乱码/很差/加载失败 → 止损，回 `MODEL_KEY = "qwen3_5_2b"`，走「保持 2B + ②批量去 LLM（MobileCLIP/MLKit 打标 + 2B 按需 summary）」

> 验证完记得把 MODEL_KEY 改回 `qwen3_5_2b`（除非决定全面切 SmolVLM）。

---

## 5. 待办（等你决策）

1. **0.8B 分支清理**：`feature-qwen35-0.8b-tag-model`（在另一个 worktree）已证伪（质量差），建议删 worktree + 分支。
2. **PhotoEditorScreen.kt 未提交改动**（main 上 `M` 状态，47 行，**不是本夜工作产生的**）：是你睡前手改的？需确认保留还是丢弃。我没动它。
3. **idle/息屏约束**：热守卫唯一缺口，可选增强。
4. **feat/tag-viewer-test-page 分支**：已合并 main，可删。
5. SmolVLM 验证结果决定是否全面接入。

---

## 6. 分支状态

| 分支 | 状态 |
|---|---|
| `main` | 2B 模型 + Tag Viewer（已合并）|
| `feat/tag-viewer-test-page` | 已合并，可删 |
| `feat/thermal-guard` | 本夜新建后已删（热守卫已存在，空分支无用）|
| `feature-qwen35-0.8b-tag-model` | 0.8B（证伪），建议清 worktree |

## 7. 未做（设备断开导致）

- SmolVLM 中文实测（明早步骤 4）
- 热守卫实际发热验证（代码已确认完善，实测要设备跑扫描观察温度）

---

*模型已就绪在 `/tmp/smolvlm2_500m_mnn/`。明早设备恢复，步骤 4 走一遍就有结论。*
