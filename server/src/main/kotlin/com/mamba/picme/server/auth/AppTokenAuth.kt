package com.mamba.picme.server.auth

/**
 * Shared auth constants for X-App-Token header validation.
 * The token value is configured via AppConfig.appToken (env: APP_TOKEN).
 */
const val APP_TOKEN_HEADER = "X-App-Token"
